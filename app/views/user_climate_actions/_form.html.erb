<%= form_with(model: user_climate_action, local: true) do |form| %>
  <% if user_climate_action.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_climate_action.errors.count, "error") %> prohibited this user_climate_action from being saved:</h2>

      <ul>
        <% user_climate_action.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :climate_action_id %>
    <%= form.text_field :climate_action_id %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.check_box :status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

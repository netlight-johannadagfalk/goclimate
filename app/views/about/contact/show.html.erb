<%= render "shared/header" %>

<section class="section-padding">
  <h1 class="heading-xl mb-6"><%= title(t('contact_us')) %></h1>
  <div class="max-w-2xl space-y-6">
    <p><%= description(t('general_questions_html')).gsub('<a', '<a class="link" rel="noopener noreferrer"').html_safe %></p>
    <p><%= t('general_questions_press_html').gsub('<a', '<a class="link" rel="noopener noreferrer"').html_safe %></p>
    <p><%= t('general_questions_facebook_html').gsub('<a', '<a class="link" rel="noopener noreferrer"').html_safe %></p>
    <p><%= t('general_questions_twitter_html').gsub('<a', '<a class="link" rel="noopener noreferrer"').html_safe %></p>
  </div>
</section>

<%= render "shared/footer" %>


<%= render 'shared/email/heading',
  text: t('mailers.data_requests.heading') %>

<%= render 'shared/email/spacer', narrow: true %>

<%= render 'shared/email/spacer', narrow: true %>

<%= render 'shared/email/paragraph',
  text: params[:message].present? ? sanitize(params[:message]) : "You have received a request to help your team collect data for GoClimate." %>

<%= render 'shared/email/spacer', narrow: true %>

<%= render 'shared/email/heading',
  text: "#{link_to 'Your link', data_reporter_url(id: params[:data_reporter].key), style: 'text-decoration: underline'}" %>

<%= render 'shared/email/spacer', narrow: true %>

<%= render 'shared/email/team_signature', name: params[:sender] || 'Alexandra' %>

          <div class="masthead clearfix">
            <div class="inner">
            <% if controller_name == "welcome" && action_name == "index" %>
              <h4 class="masthead-brand"><a href="/#why_goclimateneutral"><%=t 'why_goclimateneutral_head' %></a></h4>
              <h4 class="masthead-brand second"><a href="/#this_is_how_it_works"><%=t 'how_it_works_head' %></a></h4>
            <% else %> 
              <h4 class="masthead-brand"><a href="/">GOCLIMATENEUTRAL.ORG</a></h4>
            <% end %>


              <nav>
                <ul class="nav masthead-nav">
                  <% if user_signed_in? %> 
                      <li id="fat-menu" class="dropdown"> 
                        <a href="#" class="dropdown-toggle" id="drop3" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= current_user.user_name.nil? ? current_user.email : current_user.user_name %><span class="caret"></span> </a> 
                        <ul class="dropdown-menu" aria-labelledby="drop3"> 
                          <li><a href="<%= dashboard_index_path %>">Dashboard</a></li>
                          <li><a href="<%= edit_user_registration_path %>"><%=t 'settings' %></a></li>
                          <li role="separator" class="divider"></li> 
                          <li><%= link_to(I18n.t('log_out'), destroy_user_session_path, :method => :delete) %></li> 
                        </ul> 
                      </li> 

                  <% else %>
                    <li><%= link_to(I18n.t('log_in'), new_user_session_url)%></li>
                  <% end %>
                </ul>
              </nav>
            </div>
          </div>
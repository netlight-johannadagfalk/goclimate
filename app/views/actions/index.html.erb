<p id="notice"><%= notice %></p>

<h1>Actions</h1>

<table>
  <thead>
    <tr>
      <th>Id</th>
      <th>Name</th>
      <th>Description</th>
      <th>Points</th>
      <th>Status</th>
      <th>Repeatable</th>
      <th>Action of the month</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @actions.each do |action| %>
      <tr>
        <td><%= action.id %></td>
        <td><%= action.name %></td>
        <td><%= action.description %></td>
        <td><%= action.points %></td>
        <td><%= action.status %></td>
        <td><%= action.repeatable %></td>
        <td><%= action.action_of_the_month %></td>
        <td><%= link_to 'Show', action %></td>
        <td><%= link_to 'Edit', edit_action_path(action) %></td>
        <td><%= link_to 'Destroy', action, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Action', new_action_path %>

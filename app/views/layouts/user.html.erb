<!DOCTYPE html>
<html>
  <head>
    <title><%=t 'site_title' %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>

    <%= favicon_link_tag %>
    <%= javascript_include_tag 'application' %>
    <%= javascript_include_tag 'users' %>
    <%= stylesheet_link_tag 'application' %>
    <%= include_gon %>

    <meta property="og:type"             content="website" />
    <meta property="og:url"              content="<%=t 'goclimateneutral_url' %>users/<%= @user.id %>" />
    <meta property="og:title"              content="<%= @social_quote %>" />
    <meta property="og:description"        content="<%=t 'climate_change_is_the_greatest_challange' %>" />
    <meta property="og:image"              content="<%= webpack_asset_url('images/share-image.png') %>" />
    <meta property="fb:app_id"             content="452582378427296" />
    
    <link href="https://fonts.googleapis.com/css?family=Raleway:300" rel="stylesheet">

    <script src="https://js.stripe.com/v3/"></script>
    <script>
      (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
      (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
      m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
      })(window,document,'script','https://www.google-analytics.com/analytics.js','ga');
      ga('create', 'UA-97611261-1', 'auto');
      ga('send', 'pageview');
    </script>
  </head>

  <body class="<%= controller_name + " " + controller_name + "-" + action_name %>">
       
    <div class="site-wrapper">
       
    <% if notice || alert %>
      <div class="alert alert-warning" role="alert">
        <% if notice %>
          <p class="notice"><%= notice %></p>
        <% elsif alert %>
          <p class="alert"><%= alert %></p>
        <% end %>
      </div>
    <% end %>

    <%= yield %>

    </div>
  </body>
</html>

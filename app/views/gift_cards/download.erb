<!--
  This is the actual gift card. It can be rendered as both html and PDF, thanks
  to the magic of https://github.com/mileszs/wicked_pdf
-->
<div>
  <!--
    Best to embed the css here because PDF generating is done by a separate tool (wkhtmltopdf)
    and it's tricky to figure how that tool will find our separate css files.
   -->
  <style scoped>
      p {
        color: #444444;
        font-family: 'Raleway', sans-serif;
      }

      .content {
        position: absolute;
        top: 10px;
        width: 100%;
        text-align: center;
      }

      .logo {
        margin-top: 30px;
      }

      .congrats {
        margin-top: 30px;
        font-size: 120px;
      }

      .you-have-received {
        margin-top: 30px;
        font-size: 120px;
      }

      .message {
        margin-top: 30px;
        font-size: 90px;
      }
  </style>

  <div class="background">
    <%= image_tag wicked_pdf_asset_base64('background-illustrated.png'), size: "4100x2800"%>
  </div>

  <div class="content">
    <div class="logo">
      <%= image_tag wicked_pdf_asset_base64('goclimateneutral_logo.svg'), size: "435x250" %>
    </div>

    <div class="congrats">
      <p>Congrats, <%= session[:recipient] %>!</p>
    </div>

    <div class="you-have-received">
      <p>
        You have received <%= session[:number_of_months] %> climate neutral months via GoClimateNeutral.org
      </p>
    </div>

    <div class="message">
      <p>
        <%= session[:message] %>
      </p>
    </div>
  </div>


  <!-- TODO localize the texts below -->
  <!--
    Hardcoded full path to the image file,
    because I haven't figured out how to get relative paths to work with wkhtmltopdf
  -->
</div>

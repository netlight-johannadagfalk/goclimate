<!--
  This is the actual gift card. It can be rendered as both html and PDF, thanks
  to the magic of https://github.com/mileszs/wicked_pdf
-->
<div>
  <!--
    Best to embed the css here because PDF generating is done by a separate tool (wkhtmltopdf)
    and it's tricky to figure how that tool will find our separate css files.
   -->
  <style scoped>
      .text {
        color: #444444;
        font-family: 'Raleway', sans-serif;
        margin-top: 20px;
        margin-left: 100px;
        margin-right: 100px;

      }

      .content {
        position: absolute;
        top: 10px;
        width: 1024px;
        text-align: center;
      }

      .logo {
        margin-top: 20px;
      }

      .message {
        font-size: 20px;
        white-space: pre-wrap;
        margin-left: 200px;
        margin-right: 200px;
        border-radius: 25px;
        padding: 10px;
        margin-bottom: 50px;
      }

      .message:not(:empty) {
        background: #ffffff;
      }

      a:link, a:visited {
        color: inherit;
        text-decoration: none;
      }

  </style>

  <div class="background">
    <%= image_tag wicked_pdf_asset_base64('gift-card-background.png'), size: "1025x700"%>
  </div>

  <div class="content">
    <div class="logo">
      <%= image_tag wicked_pdf_asset_base64('goclimateneutral_logo.svg'), size: "174x100" %>
    </div>

    <div class="text" style="font-size: 35px;">
      <%=t 'views.gift_cards.download.gift_card_x_climate_neutral_months', months: @number_of_months %>
    </div>

    <div class="text" style="font-size: 20px;">
      <%= raw t 'views.gift_cards.download.via_go_climate_neutral' %>
    </div>

    <div class="text message"><%= @message %></div>

    <div class="text" style="font-size: 18px;">
      <%= raw t 'views.gift_cards.download.about_go_climate_neutral' %>
    </div>

    <div class="text" style="font-size: 18px;">
      <%= t 'views.gift_cards.download.do_you_want_to_continue' %> <br>
      <%= raw t 'views.gift_cards.download.register' %>
    </div>

  </div>
</div>

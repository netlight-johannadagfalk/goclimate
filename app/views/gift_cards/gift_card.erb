<!--
  This is the actual gift card. It can be rendered as both html and PDF, thanks
  to the magic of https://github.com/mileszs/wicked_pdf

  And yes, it needs to be called .erb rather than .html.erb, or else PDF doesn't work.

  This view uses the following variables:
    @example = true if we should include a big fat EXAMPLE stamp across the gift card
    @message = the personal message to show (can be empty)
    @number_of_months = how many months we are offsetting
-->

<div>
  <!--
    Best to embed the css here because PDF generating is done by a separate tool (wkhtmltopdf)
    and it's tricky to figure how that tool will find our separate css files.
   -->
  <style scoped>
    .text {
      color: #444444;
      font-family: 'Raleway', sans-serif;
      margin-top: 20px;
      margin-left: 100px;
      margin-right: 100px;

    }
    .content {
      /* Don't know how to use a relative url for the background setting below */
      background: url('https://www.goclimateneutral.org/gift-card-background.png');
      background-size: 1025px 700px;
      left: 0px;
      width: 1025px;
      height: 700px;
      text-align: center;
    }

    .logo {
      margin-top: 20px;
    }

    .message {
      font-size: 20px;
      white-space: pre-wrap;
      margin-left: 200px;
      margin-right: 200px;
      border-radius: 25px;
      padding: 10px;
      margin-bottom: 50px;
    }

    .message:not(:empty) {
      background: #ffffff;
    }

    a:link, a:visited {
      color: inherit;
      text-decoration: none;
    }

    .boxedIn {
      border: 2px solid darkgray;
      box-shadow: 5px 5px lightgray;
      border-radius: 10px;
    }

    .exampleStamp {
      position: relative;
      top: -200px;
      color: red;
      font-size: 110px;
      font-family: Sans-serif;
      font-weight: bold;
      opacity: 0.3;
      transform: rotate(-15deg);
    }

  </style>

  <div class="content boxedIn">
    <div class="logo">
      <%= image_tag wicked_pdf_asset_base64('goclimateneutral_logo.svg'), size: "174x100" %>
    </div>

    <div class="text" style="font-size: 35px;">
      <%=t 'views.gift_cards.download.gift_card_x_climate_neutral_months', months: @number_of_months %>
    </div>

    <div class="text" style="font-size: 20px;">
      <%= raw t 'views.gift_cards.download.via_go_climate_neutral' %>
    </div>

    <div class="text message"><%= @message %></div>

    <div class="text" style="font-size: 18px;">
      <%= raw t 'views.gift_cards.download.about_go_climate_neutral' %>
    </div>

    <div class="text" style="font-size: 18px;">
      <%= t 'views.gift_cards.download.do_you_want_to_continue' %> <br>
      <%= raw t 'views.gift_cards.download.register' %>
    </div>

    <% if @example == true %>
        <div class="exampleStamp">
          <%= t 'views.gift_cards.example.example' %>
        </div>
    <% end %>
  </div>
</div>

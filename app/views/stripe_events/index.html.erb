<p id="notice"><%= notice %></p>

<h1>Stripe Events</h1>

<table>
  <thead>
    <tr>
      <th>Stripe event</th>
      <th>Stripe customer</th>
      <th>Stripe object</th>
      <th>Stripe status</th>
      <th>Stripe amount</th>
      <th>Stripe created</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @stripe_events.each do |stripe_event| %>
      <tr>
        <td><%= stripe_event.stripe_event_id %></td>
        <td><%= stripe_event.stripe_customer_id %></td>
        <td><%= stripe_event.stripe_object %></td>
        <td><%= stripe_event.stripe_status %></td>
        <td><%= stripe_event.stripe_amount %></td>
        <td><%= stripe_event.stripe_created %></td>
        <td><%= link_to 'Show', stripe_event %></td>
        <td><%= link_to 'Edit', edit_stripe_event_path(stripe_event) %></td>
        <td><%= link_to 'Destroy', stripe_event, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Stripe Event', new_stripe_event_path %>

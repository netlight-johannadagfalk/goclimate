<p id="notice"><%= notice %></p>

<p>
  <strong>Stripe event:</strong>
  <%= @stripe_event.stripe_event_id %>
</p>

<p>
  <strong>Stripe customer:</strong>
  <%= @stripe_event.stripe_customer_id %>
</p>

<p>
  <strong>Stripe object:</strong>
  <%= @stripe_event.stripe_object %>
</p>

<p>
  <strong>Stripe status:</strong>
  <%= @stripe_event.stripe_status %>
</p>

<p>
  <strong>Stripe amount:</strong>
  <%= @stripe_event.stripe_amount %>
</p>

<p>
  <strong>Stripe created:</strong>
  <%= @stripe_event.stripe_created %>
</p>

<%= link_to 'Edit', edit_stripe_event_path(@stripe_event) %> |
<%= link_to 'Back', stripe_events_path %>

<%= form_with(model: [:admin, organization], local: true, class: 'space-y-3') do |form| %>
  <% if organization.errors.any? %>
    <div>
      <h2 class="heading"><%= pluralize(organization.errors.count, "error") %> prohibited this organization from being saved:</h2>

      <ul class="list-bullet">
        <% organization.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="flex flex-col">
    <%= form.label :name, class: 'font-bold' %>
    <%= form.text_field :name, class: 'input' %>
  </div>

  <div class="text-right">
    <%= form.submit class: 'button button-cta' %>
  </div>
<% end %>

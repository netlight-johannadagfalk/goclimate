<%= form_with(model: [:admin, climate_action_category], local: true) do |form| %>
  <% if climate_action_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(climate_action_category.errors.count, "error") %> prohibited this climate_action_category from being saved:</h2>
      <ul>
        <% climate_action_category.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="flex flex-col">
    <%= form.label :name, class: 'font-bold' %>
    <%= form.text_field :name, class: 'input' %>
  </div>

  <div class="flex flex-col">
    <%= form.label :description, class: 'font-bold' %>
    <%= form.text_field :description, class: 'input' %>
  </div>

  <div class="flex flex-col">
    <h3>Category badge</h3>
  </div>

  <div class="flex flex-col">
    <%= form.label :badge_name, class: 'font-bold' %>
    <%= form.text_field :badge_name, class: 'input' %>
  </div>

  <div class="flex flex-col">
    <%= form.label :badge_description, class: 'font-bold' %>
    <%= form.text_field :badge_description, class: 'input'%>
  </div>

  <div class="flex flex-col">
    <%= form.label :badge_image_url, class: 'font-bold' %>
    <%= form.text_field :badge_image_url, class: 'input' %>
  </div>

  <%= form.submit class: 'button button-cta mt-10' %>
<% end %>

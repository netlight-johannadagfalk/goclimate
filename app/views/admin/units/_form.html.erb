<%= form_with(model: [:admin, unit], local: true) do |form| %>
  <% if unit.errors.any? %>
    <div>
      <h2><%= pluralize(unit.errors.count, "error") %> prohibited this unit from being saved:</h2>

      <ul>
        <% unit.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :name %>
    <%= form.text_field :name, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :key %>
    <%= form.text_field :key, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :input_type %>
    <%= form.select(:input_type, [['Number field', 'number'], ['Text area', 'textarea'], ['Text field', 'text']], { selected: form.object.input_type }, { class: 'form-control' }) %>
  </div>

  <%= form.submit class: 'btn btn-primary' %>
<% end %>

<h1 class="heading-lg">New Data Reporter for <%= @report_area&.title %></h1>
<h3>Report <%= @report.title %></h3>

<%= render 'form', reporter: @reporter %>



<%= link_to 'Back', admin_reporters_path %>
